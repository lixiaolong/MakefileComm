# may need change
TARGET = my_app
CC = gcc
INCLUDES = -I./
LIBS = -L./
CCFLAGS = -g -Wall -O0

# for c
SRCS = $(wildcard *.c)
OBJS = $(SRCS:.c = .o)

###################
$(TARGET) : $(OBJS)
	$(CC) $^ -o $@ $(INCLUDES) $(LIBS)

%.o : %.c
	$(CC) -c $< $(CCFLAGS)

clean:
	rm *.o
.PHONY:clean
